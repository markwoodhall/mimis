(fn depends []
  [:modules.cmdline.docker
   :modules.cmdline.aws
   :modules.cmdline.npm
   :modules.cmdline.common
   :modules.cmdline.chatgpt])

(fn enable [])
(fn setup [])

{: enable
 : setup
 : depends }
